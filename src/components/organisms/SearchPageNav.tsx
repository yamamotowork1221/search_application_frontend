import { NavServices } from '@/components';

export const SearchPageNav = () => {
    return (
        <section className="flex justify-evenly items-center border-b-1 border-blue-500">
            <NavServices />
        </section>
    );
};